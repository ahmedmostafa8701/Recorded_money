<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/cash_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cash_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecordedMoney"
        tools:targetApi="31">
        <activity
            android:name=".features.home_page.presentation.pages.HomePageActivity"
            android:exported="true"
            android:label="@string/home_page_title"
            android:theme="@style/Theme.RecordedMoney">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>